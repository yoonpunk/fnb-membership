# database & jpa
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/fnb?serverTimezone=UTC&characterEncoding=UTF-8
    username: fnb
    password: fnb123

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

# logging
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
#        type:
#          descriptor:
#            sql:
#              BasicBinder: TRACE

# swagger-ui custom path
springdoc:
  swagger-ui:
    path: /swagger-ui.html